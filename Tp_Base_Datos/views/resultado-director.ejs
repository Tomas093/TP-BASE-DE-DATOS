<!DOCTYPE html>
<html lang="es"></html>
<head>
    <meta charset="UTF-8">
    <title>Resultados de Búsqueda de Director</title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="icon" href="views/img/logo.png" type="image/png">
</head>
<body>
    <header class="header">
        <a href="/" class="logo">
          <img src="views/img/logo.png" alt="logo" width="55px">
        </a>
        <i class='bx bx-menu' id="menu-icon"></i>
        <nav class="navbar">
          <a href="/movies" class="nav-link">Películas</a>
          <a href="/directors" class="nav-link">Directores</a>>
          <a href="/actors" class="nav-link">Actores</a>
          <% if (user) { %>
              <a class="nav-link"><%= user.name %></a>
              <a href="/logout" class="nav-link">Cerrar Sesión</a>
          <% } else { %>
              <a href="/login" class="nav-link">Iniciar Sesión</a>
          <% } %>
        </nav>
    </header>
    <h1>Resultados de la Búsqueda de Director</h1>

    <!-- Mostrar el mensaje sobre el director -->
    <p><%= message %></p>

    <% if (movies.length === 0) { %>
        <p>No hay resultados.</p>
    <% } else { %>
        <ul>
            <% movies.forEach((movie) => { %>
                <li><a href="/pelicula/<%= movie.movie_id %>"><%= movie.title %></a></li>
            <% }); %>
        </ul>
    <% } %>

    <div id="inicio">
        <a href="/" class="boton-inicio">Inicio</a>
    </div>
</body>
</html>
